# ============================================
# TASKLETS PRODUCTION ENVIRONMENT
# Copy this to .env.prod and fill in values
# ============================================

# JWT Secret (min 32 characters)
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars

# Database URL (adjust host for your PostgreSQL container)
# For Podman: use host.containers.internal or container IP
DATABASE_URL=postgresql://postgres:your-db-password@host.containers.internal:5432/tasklets

# CORS Origins (comma separated)
ALLOWED_ORIGINS=http://localhost:4010,http://localhost:4020
